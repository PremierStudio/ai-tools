---
alwaysApply: true
---

Strict TypeScript: `noUnusedLocals`, `noUnusedParameters`, `noUncheckedIndexedAccess` all enabled. No `any` — `typescript/no-explicit-any` is an error in oxlint. No `_variable` prefix for unused params — use `void variable` instead.
